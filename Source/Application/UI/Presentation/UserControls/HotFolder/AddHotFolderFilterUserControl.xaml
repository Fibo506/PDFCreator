<UserControl x:Class="pdfforge.PDFCreator.UI.Presentation.UserControls.HotFolder.AddHotFolderFilterUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:designTime="clr-namespace:pdfforge.PDFCreator.UI.Presentation.DesignTime"
             xmlns:local="clr-namespace:pdfforge.PDFCreator.UI.Presentation.UserControls.HotFolder"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance designTime:DesignTimeAddHotFolderFilterViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Translation.EnterFilterText}"/>

        <TextBox Grid.Row="1" 
                 Margin="0 10 0 0"
                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Row="2"
               Content="{Binding ErrorMessage}"
               Foreground="Red"
               FontSize="10"
               Visibility="{Binding IsExtensionInvalid, Converter={StaticResource BoolToVisibility}}"/>

        <StackPanel Grid.Row="3" 
                    Margin="0 20 0 0"
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="{Binding Translation.Cancel}" 
                    Command="{Binding CancelCommand}"
                    Margin="0 0 10 0"
                    MinWidth="80"/>
            <Button Content="Save" 
                    Command="{Binding SaveCommand}"
                    MinWidth="80"/>
        </StackPanel>
    </Grid>
</UserControl>
