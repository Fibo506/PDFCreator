<UserControl x:Class="pdfforge.PDFCreator.UI.Presentation.UserControls.BusinessFeatureBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converter="clr-namespace:pdfforge.PDFCreator.UI.Presentation.Converter"
             xmlns:designTime="clr-namespace:pdfforge.PDFCreator.UI.Presentation.DesignTime"
             xmlns:local="clr-namespace:pdfforge.PDFCreator.UI.Presentation.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance designTime:DesignTimeBusinessFeatureBadgeViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/ColorDictionary.xaml" />
                <ResourceDictionary Source="../../Styles/DefaultsDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="local:BusinessFeatureBadge">
                <Setter Property="Edition" Value="{Binding Edition, Mode=TwoWay}" />
            </Style>
            <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" TrueValue="Visible" FalseValue="Collapsed" />
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Visibility" Value="{Binding ShowBusinessHint, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Style>
    </UserControl.Style>

    <Button Cursor="{x:Static Cursors.Hand}"
            Style="{x:Null}"
            Command="{Binding ShowEditionWebsiteCommand}"
            IsEnabled="{Binding ShowBusinessHint}"
            VerticalAlignment="Center"
            HorizontalAlignment="Left">
        <Button.Template>
            <ControlTemplate TargetType="{x:Type ContentControl}">
                <ContentPresenter Content="{TemplateBinding Content}" />
            </ControlTemplate>
        </Button.Template>

        <ContentControl
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Background="{StaticResource WhiteBrush}"
            Foreground="{StaticResource DefaultTextColorBrush}"
            FontSize="9"
            FontWeight="Bold"
            ToolTip="{Binding ToolTip}"
            IsTabStop="False">
            <ContentControl.Template>
                <ControlTemplate TargetType="ContentControl">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource AccentGrey}"
                            BorderThickness="1"
                            Padding=" 5 2"
                            CornerRadius="2">
                        <StackPanel Orientation="Horizontal">
                            <ContentControl x:Name="lightningIcon"
                                          Content="{StaticResource BusinessEditionLightningIcon}"
                                          Width="9"
                                          Height="13"
                                          Margin="0,0,4,0"/>
                            <TextBlock
                                Text="{TemplateBinding Content}"
                                Foreground="{TemplateBinding Foreground}"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BusinessEditionOrangeBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource DefaultTextColorBrush}"/>
                            <Setter TargetName="lightningIcon" Property="Content" Value="{StaticResource BusinessEditionBlackLightningIcon}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ContentControl.Template>
            <ContentControl.Content>
                <PriorityBinding>
                    <Binding Path="FeatureText" />
                    <Binding Source="PREMIUM FEATURE" />
                </PriorityBinding>
            </ContentControl.Content>
        </ContentControl>
    </Button>
</UserControl>
