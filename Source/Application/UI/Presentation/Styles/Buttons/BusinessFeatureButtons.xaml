<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:profiles="clr-namespace:pdfforge.PDFCreator.UI.Presentation.UserControls.Profiles"
                    xmlns:presentation="clr-namespace:pdfforge.PDFCreator.UI.Presentation">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../ColorDictionary.xaml" />
        <ResourceDictionary  Source="../../Styles/Buttons/DefaultButtons.xaml" />
    </ResourceDictionary.MergedDictionaries>


    <Style x:Key="MainBusinessOrangeButton" TargetType="{x:Type Button}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontFamily" Value="{StaticResource Inter}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource DefaultTextColorBrush}"/>
        <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
        <Setter Property="Padding" Value="10 5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource AccentGrey}"
                            BorderThickness="1"
                            Padding="{TemplateBinding Padding}">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <ContentControl x:Name="lightningIcon"
                                            Content="{StaticResource BusinessEditionLightningIcon}"
                                            Width="11"
                                            Height="16"
                                            Margin="0,0,8,0" />
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter Property="Background"
                                    Value="{StaticResource BusinessEditionOrangeBrush}" />
                            <Setter TargetName="lightningIcon"
                                    Property="Content"
                                    Value="{StaticResource BusinessEditionBlackLightningIcon}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AddActionButtonWithBusinessFeatureHighlight"
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource DefaultButton}">
        <Setter Property="Focusable"
                Value="False" />
        <Setter Property="Margin"
                Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="border"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}">
                        <Border.OpacityMask>
                            <VisualBrush>
                                <VisualBrush.Visual>
                                    <Border SnapsToDevicePixels="True"
                                            Background="Black"
                                            BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=Border}}"
                                            CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=Border}}"
                                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                                            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}" />
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.OpacityMask>

                        <Grid>
                            <!-- Mouse Animation Layer -->
                            <Grid x:Name="hoverLayer"
                                  Background="Black"
                                  Opacity="0">
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Grid.Opacity)"
                                                                 From="0"
                                                                 To="0.1"
                                                                 Duration="0:0:0.15" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Grid.Opacity)"
                                                                 From="0.1"
                                                                 To="0"
                                                                 Duration="0:0:0.15" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Grid.Triggers>
                            </Grid>

                            <!-- Content with optional icon -->
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Margin="{TemplateBinding Padding}"
                                        IsHitTestVisible="false">
                                <ContentControl x:Name="lightningIcon"
                                                Content="{StaticResource BusinessEditionLightningIcon}"
                                                Width="11"
                                                Height="16"
                                                Margin="0,0,8,0"
                                                Visibility="Collapsed" />
                                <ContentPresenter RecognizesAccessKey="True">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming"
                                                    Value="CharacterEllipsis" />
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                            </StackPanel>

                            <!-- Disabled Layer -->
                            <Grid Background="White"
                                  IsHitTestVisible="False">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled"
                                                     Value="True">
                                                <Setter Property="Opacity"
                                                        Value="0" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled"
                                                     Value="False">
                                                <Setter Property="Opacity"
                                                        Value="0.4" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Show icon for business features when highlighted -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsBusinessFeature}"
                                           Value="True" />
                                <Condition Binding="{Binding DataContext.IsBusinessFeaturesHighlighted, RelativeSource={RelativeSource AncestorType=profiles:AddActionOverlayView}}"
                                           Value="true" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="lightningIcon"
                                        Property="Visibility"
                                        Value="Visible" />
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{StaticResource WhiteBrush}" />
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{StaticResource AccentGrey}" />
                                <Setter TargetName="border"
                                        Property="BorderThickness"
                                        Value="2" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>

                        <!-- Hover effect for business features: Yellow background with black icon -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsBusinessFeature}"
                                           Value="True" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}"
                                           Value="True" />
                                <Condition Binding="{Binding DataContext.IsBusinessFeaturesHighlighted, RelativeSource={RelativeSource AncestorType=profiles:AddActionOverlayView}}"
                                           Value="true" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter Property="Foreground"
                                        Value="{StaticResource DefaultTextColorBrush}" />
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{StaticResource BusinessEditionOrangeBrush}" />
                                <Setter TargetName="border"
                                        Property="BorderThickness"
                                        Value="0" />
                                <Setter TargetName="lightningIcon"
                                        Property="Content"
                                        Value="{StaticResource BusinessEditionBlackLightningIcon}" />
                                <Setter TargetName="hoverLayer"
                                        Property="Opacity"
                                        Value="0" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BusinessEditionMainShellButton" TargetType="{x:Type Button}" BasedOn="{StaticResource DefaultButton}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="{x:Type ButtonBase}">
                <Border
                    x:Name="border"
                    VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                    CornerRadius="{TemplateBinding Border.CornerRadius}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Background="{TemplateBinding Background}">
                    <Border.OpacityMask>
                        <VisualBrush>
                            <VisualBrush.Visual>
                                <Border SnapsToDevicePixels="True"
                                        Background="Black"
                                        BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=Border}}"
                                        CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=Border}}"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                                        Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Border.OpacityMask>

                    <Grid>
                        <!-- Mouse Animation Layer -->
                        <Grid x:Name="hoverLayer" Background="Black" Opacity="0">
                            <Grid.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="(Grid.Opacity)"
                                                From="0" To="0.1" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="(Grid.Opacity)"
                                                From="0.1" To="0" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Grid.Triggers>
                        </Grid>

                        <!-- Content with optional icon -->
                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"
                                    IsHitTestVisible="false">
                            <ContentControl x:Name="lightningIcon"
                                          Content="{StaticResource BusinessEditionLightningIcon}"
                                          Width="11"
                                          Height="16"
                                          Margin="0,0,8,0"
                                          Visibility="Collapsed"/>
                            <ContentPresenter RecognizesAccessKey="True">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </StackPanel>

                        <!-- Disabled Layer -->
                        <Grid Background="White" IsHitTestVisible="False">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="True">
                                            <Setter Property="Opacity" Value="0" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.4" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                        </Grid>
                    </Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <!-- Show icon for free edition -->
                    <DataTrigger Binding="{Binding IsFreeEdition}" Value="True">
                        <Setter TargetName="lightningIcon" Property="Visibility" Value="Visible"/>
                        <Setter TargetName="border" Property="Background" Value="{StaticResource WhiteBrush}"/>
                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentGrey}"/>
                        <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                        <Setter Property="Foreground" Value="{StaticResource DefaultTextColorBrush}"/>
                    </DataTrigger>
                    
                    <!-- Hover effect for free edition: Yellow background with black icon -->
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsFreeEdition}" Value="True"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                        </MultiDataTrigger.Conditions>
                        <MultiDataTrigger.Setters>
                            <Setter Property="Foreground" Value="{StaticResource DefaultTextColorBrush}"/>
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BusinessEditionOrangeBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="lightningIcon" Property="Content" Value="{StaticResource BusinessEditionBlackLightningIcon}"/>
                            <Setter TargetName="hoverLayer" Property="Opacity" Value="0"/>
                        </MultiDataTrigger.Setters>
                    </MultiDataTrigger>
                     <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsFreeEdition}" Value="True"/>
                        <Condition Binding="{Binding ActivePath}" Value="{x:Static presentation:RegionViewName.HotFolderView}"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Foreground" Value="{StaticResource DefaultTextColorBrush}"/>
                        <Setter TargetName="border" Property="Background" Value="{StaticResource BusinessEditionOrangeBrush}"/>
                        <Setter TargetName="border" Property="BorderThickness" Value="0"/>
                        <Setter TargetName="lightningIcon" Property="Content" Value="{StaticResource BusinessEditionBlackLightningIcon}"/>
                        <Setter TargetName="hoverLayer" Property="Opacity" Value="0"/>
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

    <Style x:Key="BusinessEditionPreviewButton" TargetType="{x:Type Button}" BasedOn="{StaticResource PreviewPageManipulationButton}">
        <Style.Triggers>

            <DataTrigger Binding="{Binding IsFreeEdition}" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource MainGreenGradient}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
                <Setter Property="Background" Value="{StaticResource FadedMainGreenGradient}"/>
                <!--<Setter Property="IsEnabled" Value="False"/>-->
                <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
                <Setter Property="ToolTipService.IsEnabled" Value="True"/>
                <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
                <!--<Setter Property="ToolTipService.ToolTip" Value="Business Feature"/>-->
            </DataTrigger>

        </Style.Triggers>
    </Style>

</ResourceDictionary>
